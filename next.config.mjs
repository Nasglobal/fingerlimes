/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "fingerlimes.com","uplifting-delight-9f7c5ff1c7.strapiapp.com"],
  },
};

export default nextConfig;